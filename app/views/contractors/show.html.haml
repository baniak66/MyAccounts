%h3 Contractor details:
%p Name: #{contractor.name}
%p Address: #{contractor.address}
%p NIP: #{contractor.nip}
%p Added: #{contractor.created_at.strftime("%F")}

%h3 Transactions with #{contractor.name}:
%table.table
  %tr
    %th Number
    %th Date
    %th Amount
    %th Description
    %th
  -contractor.evidences.each do |evidence|
    %tr
      %td= evidence.number
      %td= evidence.madedate
      %td= evidence.amount
      %td= evidence.description
      %td= link_to "Details", evidence, class: "btn btn-xs btn-default"
